# dbtx Context

## Domain Terms

### Dbt process

A spawned `dbt` command run by dbtx. Normal invocation execution and validation both use Dbt process handling for executable resolution, stdout/stderr capture, exit-code interpretation, and cancellation behavior.
